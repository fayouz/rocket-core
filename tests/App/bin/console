#!/usr/bin/env php
<?php
// Console of the test application: php tests/App/bin/console doctrine:migrations:migrate…

use Rocket\Core\Tests\App\Kernel;
use Symfony\Bundle\FrameworkBundle\Console\Application;
use Symfony\Component\Dotenv\Dotenv;

require dirname(__DIR__, 3).'/vendor/autoload.php';
(new Dotenv())->bootEnv(dirname(__DIR__).'/.env');

exit((new Application(new Kernel($_SERVER['APP_ENV'] ?? 'test', true)))->run());
